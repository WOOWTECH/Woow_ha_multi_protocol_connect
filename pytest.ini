[pytest]
# Hermetic tests: the Service layer (tests/services) and the config/setup seam
# (tests/config). The live-integration suites (tests/live/live_*.py) are
# standalone scripts and are NOT collected here.
testpaths = tests/services tests/config
pythonpath = .
asyncio_mode = auto
