[pytest]
# Hermetic unit tests for the Service layer. The live-integration suites
# (tests/live/live_*.py) are standalone scripts and are NOT collected here.
testpaths = tests/services
pythonpath = .
asyncio_mode = auto
