[pytest]
# Hermetic unit tests for the Service layer. The live-integration suites
# (test_enterprise.py etc.) are standalone scripts and are NOT collected here.
testpaths = tests/services
pythonpath = .
asyncio_mode = auto
